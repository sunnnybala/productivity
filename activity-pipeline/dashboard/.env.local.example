# Copy to .env.local (and set the same in Vercel project env). SERVER-ONLY — never NEXT_PUBLIC.
SUPABASE_URL=https://myyfhbmydhkkqpqchcka.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service_role key from Supabase -> Project Settings -> API>
DASH_PASSWORD=<a LONG, high-entropy shared passphrase (not a simple word)>
DASH_TOKEN=<a long random string; the session cookie value>
